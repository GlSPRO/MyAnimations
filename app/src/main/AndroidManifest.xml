<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myanymations">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- MainActivity with explicit android:exported attribute -->
        <activity
            android:name=".MainActivity"
            android:exported="true"> <!-- Explicitly specify exported attribute -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Other activities -->
        <activity android:name=".FrameAnimationActivity" android:exported="false" />
        <activity android:name=".TweenAnimationActivity" android:exported="false" />

    </application>
</manifest>
